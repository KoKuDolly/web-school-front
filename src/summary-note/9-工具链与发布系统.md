# 工具链（初始化与构建，单元测试工具） 与 发布系统 与 持续集成

## 工具链

1. yeoman
2. generator
3. webpack
4. babel
5. mocha/jest
6. code coverage nyc
7. 对 html-parser 进行单元测试，并 code coverage 达到 90%以上
8. 所有工具与 generator 结合，就是在 yeoman 的 templates 放需要的模板

## 持续集成 git hooks lint 无头浏览器检查 DOM

## 发布系统

web server

web publish

stream readable writeable

多文件发布

github oAuth 做一个登录实例

## headless

基于 dom 的单元测试，用 puppeteer.js 写一个框架

## lint

```js
// 子进程的理解
const child_process = requrie('child_process')
```

eslint stylelint commitlint editorconfig

git stash push -k

git stash pop

npx eslint --file

git commit -m 'feat: xxx'
